{
	"name": "nextcloud/smartmigration",
	"description": "<![CDATA[Provides a Nextcloud interface for defining and monitoring MigrateDMS
SMART Migration jobs.

Create a job for each SharePoint, OneDrive or Teams document library — or
file share — and run deep file discovery before you move anything. Interactive
business intelligence shows exactly what's there: volume, age, structure and
the content that needs attention first. You plan the migration on a complete
picture of your data.

The same reporting tracks progress while the migration runs, and becomes your
documentation of what was moved once it's done.

Discovery, migration and run history all stay visible in Nextcloud, with
reports delivered to Nextcloud Files.

Requires SMART Migration. See https://www.migratedms.com]]>",
	"license": "AGPL-3.0-or-later",
	"authors": [
		{
			"name": "Steen Jakobsen",
			"email": "sj@migratedms.com",
			"homepage": "https://migratedms.com"
		}
	],
	"autoload": {
		"psr-4": {
			"OCA\\SmartMigration\\": "lib/"
		}
	},
	"scripts": {
		"post-install-cmd": [
			"@composer bin all install --ansi"
		],
		"post-update-cmd": [
			"@composer bin all install --ansi"
		],
		"lint": "find . -name \\*.php -not -path './vendor/*' -not -path './vendor-bin/*' -not -path './build/*' -print0 | xargs -0 -n1 php -l",
		"cs:check": "php-cs-fixer fix --dry-run --diff",
		"cs:fix": "php-cs-fixer fix",
		"psalm": "psalm --threads=1 --no-cache",
		"test:unit": "phpunit tests -c tests/phpunit.xml --colors=always --fail-on-warning --fail-on-risky",
		"openapi": "generate-spec",
		"rector": "rector && composer cs:fix"
	},
	"require": {
		"bamarni/composer-bin-plugin": "^1.8",
		"php": "^8.1"
	},
	"require-dev": {
		"nextcloud/ocp": "dev-stable31",
		"roave/security-advisories": "dev-latest"
	},
	"config": {
		"allow-plugins": {
			"bamarni/composer-bin-plugin": true
		},
		"optimize-autoloader": true,
		"sort-packages": true,
		"platform": {
			"php": "8.1"
		}
	}
}
